{
  "name": "mythos-writer",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "electron-main"
  ],
  "scripts": {
    "dev": "electron-vite dev",
    "build": "npm run build -w electron-main && npm run build -w frontend && npm run package",
    "package": "electron-builder --win",
    "lint": "npm run lint -w frontend && npm run lint -w electron-main",
    "test": "npm run test -w frontend && npm run test -w electron-main",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w electron-main",
    "build:electron": "npm run build -w electron-main && npm run build -w frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "electron": "^33.2.0",
    "electron-vite": "^2.3.0",
    "electron-builder": "^25.1.8",
    "electron-updater": "^6.3.9"
  }
}
