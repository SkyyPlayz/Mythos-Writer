{
  "name": "mythos-writer",
  "version": "0.1.0",
  "private": true,
  "main": "out/main/main.js",
  "workspaces": [
    "frontend",
    "electron-main"
  ],
  "scripts": {
    "dev": "electron-vite dev --config electron-vite.config.ts",
    "build": "electron-vite build --config electron-vite.config.ts && electron-builder --win",
    "package": "electron-builder --win",
    "lint": "npm run lint -w frontend && npm run lint -w electron-main",
    "test": "npm run test -w electron-main && npm run test -w frontend",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w electron-main",
    "build:electron": "electron-vite build --config electron-vite.config.ts"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-vite": "^2.3.0",
    "electron-builder": "^25.1.8",
    "electron-updater": "^6.3.9"
  }
}
